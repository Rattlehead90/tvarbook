<div class="col-span-2 mt-5 w-[590px] justify-self-center">
  <%= render 'post_prompt' %>
  <div>
    <%= render "posts", posts: @posts %>
  </div>
  <% unless !params[:query].nil? %>
    <div class="pagination b-0"><%= paginate @posts %></div>
  <% end %>
</div>